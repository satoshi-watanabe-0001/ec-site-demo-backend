server:
  port: 8080

spring:
  application:
    name: ${SERVICE_NAME:product-category-service}
  jackson:
    property-naming-strategy: SNAKE_CASE
  
  datasource:
    url: jdbc:postgresql://localhost:5432/${DB_NAME:ahamo_dummy_demo2}
    username: ${DB_USERNAME:ahamo_user}
    password: ${DB_PASSWORD:ahamo_password}
    driver-class-name: org.postgresql.Driver
    
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
        
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
    
  security:
    user:
      name: admin
      password: admin
      roles: ADMIN

# JWT設定
jwt:
  secret: ${JWT_SECRET}
  access-token-expiration: ${JWT_ACCESS_TOKEN_EXPIRATION:3600000}  # 1時間（ミリ秒）
  refresh-token-expiration: ${JWT_REFRESH_TOKEN_EXPIRATION:86400000} # 24時間（ミリ秒）

# Rate Limiting Configuration (Optional - uncomment if needed)
# rate-limit:
#   login-attempts: 5
#   lockout-duration: 300000 # 5 minutes in milliseconds

# Logging Configuration
logging:
  level:
    com.example.ec: INFO
    org.springframework.security: DEBUG
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"
    file: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"

# Management endpoints
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: when-authorized
